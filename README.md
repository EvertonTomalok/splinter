# splinter
A multiagent coding harness where one model plans and cheaper models execute, with an automatic judge that escalates to pricier models only when needed. Pick a strategy (long run or direct), feed it a PRD, and let it loop until the acceptance criteria pass. Pay for frontier intelligence only when the work actually demands it.
